<?xml version="1.0" encoding="utf-8"?>
<!--db-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/main_color_VERYDARK"
    android:layout_gravity="center">

    <ImageView
        android:id="@+id/im_book_image_splash_screen"
        android:layout_width="wrap_content"
        android:layout_height="80dp"
        android:src="@drawable/tutor_icon"
        android:layout_marginBottom="20dp"
        app:layout_constraintBottom_toTopOf="@id/im_logo_splash_screen"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <ImageView
        android:id="@+id/im_logo_splash_screen"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/logo_normal"/>

    <TextView
        android:id="@+id/tv_motto_splash_screen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/mottoLabel"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textSize="16sp"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="60dp"
        app:layout_constraintTop_toBottomOf="@id/im_logo_splash_screen"
        app:layout_constraintStart_toStartOf="@id/im_logo_splash_screen"
        app:layout_constraintEnd_toEndOf="@id/im_logo_splash_screen"
        />

</androidx.constraintlayout.widget.ConstraintLayout>